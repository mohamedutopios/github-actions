# ============================================================
# DÉMO 1/5 — Déclencheur : push simple
# ============================================================
# Ce workflow se lance à CHAQUE git push,
# sur N'IMPORTE QUELLE branche.
# C'est la forme la plus minimaliste possible.
#
# Testez en faisant : git push
# Puis allez dans l'onglet Actions de votre dépôt.
# ============================================================

name: "01 · Push simple"

on: push

jobs:
  afficher-info:
    runs-on: ubuntu-latest
    steps:
      - name: 📣 Afficher les infos du push
        run: |
          echo "✅ Push détecté !"
          echo "👤 Auteur  : ${{ github.actor }}"
          echo "🌿 Branche : ${{ github.ref_name }}"
          echo "🔖 Commit  : ${{ github.sha }}"
