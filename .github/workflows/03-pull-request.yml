# ============================================================
# DÉMO 3/5 — Déclencheur : pull_request
# ============================================================
# Ce workflow se déclenche quand une Pull Request est
# ouverte ou mise à jour, ciblant la branche "main".
#

# C'est le déclencheur idéal pour valider du code
# AVANT qu'il soit fusionné dans main.
#

# Essayez :
#   1. Créez une branche : git checkout -b feature/test
#   2. Faites un commit et poussez
#   3. Ouvrez une Pull Request vers main sur GitHub
#   → Le workflow se lance automatiquement ✅
# ============================================================

name: "03 · Pull Request vers main"

on:
  pull_request:
    branches:
      - main

jobs:
  validation:
    runs-on: ubuntu-latest
    steps:
      - name: 🔍 Inspecter la PR
        run: |
          echo "📋 PR ouverte ou mise à jour"
          echo "👤 Auteur       : ${{ github.actor }}"
          echo "🌿 Branche PR   : ${{ github.head_ref }}"
          echo "🎯 Cible        : ${{ github.base_ref }}"
