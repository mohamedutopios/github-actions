# ============================================================
# DÉMO 2/5 — Déclencheur : push filtré par branche
# ============================================================
# On affine : le workflow ne se déclenche QUE pour les
# pushs vers "main" ou "develop". Les autres branches
# sont ignorées.
#
# Essayez :
#   git push origin feature/test   → workflow ignoré ❌
#   git push origin main           → workflow lancé  ✅
# ============================================================

name: "02 · Push filtré (main & develop)"

on:
  push:
    branches:
      - main
      - develop

jobs:
  afficher-info:
    runs-on: ubuntu-latest
    steps:
      - name: 📣 Confirmer le déclenchement
        run: |
          echo "✅ Push reçu sur une branche surveillée"
          echo "🌿 Branche : ${{ github.ref_name }}"
